#ifndef NETWORK_H_
#define NETWORK_H_

#include <Arduino.h>
#include <IPAddress.h>

const char ssid[]       = "0";
const char pass[]       = "0";
IPAddress pcIP(0, 0, 0, 0);
unsigned int port = 0;

#endif